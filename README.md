# 📘 Exercícios em JavaScript (Node.js)

Este repositório contém uma coleção de pequenos programas desenvolvidos em **JavaScript**, utilizando **Node.js** e o módulo nativo `readline` para interação com o usuário via terminal.  
Os exercícios têm como objetivo praticar lógica de programação, manipulação de dados e estruturas básicas da linguagem.

---

## 📂 Arquivos incluídos

| Arquivo | Descrição |
|--------|-----------|
| `concat_dados.js` | Concatena dois dados fornecidos pelo usuário. |
| `mat_simples.js` | Realiza operações matemáticas básicas entre dois números. |
| `media.js` | Calcula a média de três notas. |
| `par_impar.js` | Verifica se um número é par ou ímpar. |
| `polinidromo.js` | Testa se uma palavra é um palíndromo. |
| `repet_texto.js` | Repete uma string um número determinado de vezes. |

---

## 🧩 Descrição dos Programas

### 🔹 `concat_dados.js`
Solicita dois valores ao usuário e retorna a concatenação deles em uma única string.

---

### 🔹 `mat_simples.js`
Recebe dois números e realiza as operações:

- Soma  
- Subtração  
- Multiplicação  
- Divisão (com tratamento para divisão por zero)

Inclui validação para garantir que os valores inseridos são numéricos.

---

### 🔹 `media.js`
Calcula a média aritmética de três notas fornecidas pelo usuário.

- Converte as entradas para número  
- Valida se são valores válidos  
- Exibe a média com duas casas decimais  

---

### 🔹 `par_impar.js`
Verifica se um número inteiro é **par** ou **ímpar**.

- Conversão para inteiro  
- Validação da entrada  
- Exibição do resultado  

---

### 🔹 `polinidro.js`
Determina se uma palavra é um **palíndromo**, ignorando:

- Espaços  
- Pontuação  
- Diferenças entre maiúsculas e minúsculas  

---

### 🔹 `repet_texto.js`
Repete uma string um número determinado de vezes.

- Recebe uma string e um número inteiro  
- Valida a entrada  
- Usa o método `.repeat()`  
- Retorna o texto repetido  

---

## ▶️ Como executar

Certifique-se de ter o **Node.js** instalado.

No terminal, execute:

```bash
node nome_do_arquivo.js


